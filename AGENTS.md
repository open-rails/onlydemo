# Working agreements

- Work directly on `master` in this repository's primary checkout.
- Do not create task branches, worktrees, or pull requests for this demo.
- Commit and push demo changes directly to `master`.
- Preserve unrelated uncommitted and staged work.

This repository-specific policy overrides the general task-branch and worktree workflow.
